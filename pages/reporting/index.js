/**
 * /reporting used to render a second, thinner sales dashboard that showed a
 * subset of what /reporting/reporting already shows, and nothing in the menu
 * linked to it — only stray breadcrumbs did. It now sends people to the real
 * Sales Report so there is one place to look.
 */
export { default } from "@/pages/reporting/reporting";
